# Canonical Paths

Updated: {{DATE}}

| Concern | Inspect first |
|---|---|
| <!-- e.g. API and readiness --> | <!-- fill in path(s) --> |
| <!-- e.g. Main execution --> | <!-- fill in path(s) --> |
| <!-- e.g. Data/signal ingestion --> | <!-- fill in path(s) --> |
| Tests | `tests/` |
| Deployment | <!-- e.g. `.github/`, `Dockerfile*`, `docker-compose*.yml` --> |
| AI workflow memory | `docs/ai/` |

Do not infer ownership from filenames alone; verify imports and runtime
callers.
